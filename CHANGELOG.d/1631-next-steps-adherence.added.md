`_meta.next_steps_adherence` surfaces in-conversation adherence telemetry on every response after the first emission (#1631 v0.85). Tracks emitted vs followed `next_steps[]` recommendations per session, exposed as `{emitted, followed, pct}` so dashboards can surface the outcome metric that pairs with #1635's hook-boundary conversion rate. In-memory only this iteration; persistent surfacing across restarts is filed as a follow-up.
