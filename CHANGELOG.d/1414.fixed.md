MCP `init` tool description now names every registered target — pre-fix the jetbrains (#1335, shipped v0.71) and antigravity (#1385/#1368, shipped v0.72) targets worked but never appeared in either the outer tool description or the `target` param's enum-style list, so an agent reading the schema would never think to ask for them. New `TestInitTool_DescriptionMentionsEveryTarget` mechanically pins description-vs-registry parity going forward — same shape as the existing tool-contract gate. (#1414)
