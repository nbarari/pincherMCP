`context_for_task` no longer emits an unbounded response that exceeds the MCP per-call token cap (#1729 v0.89). The composite traces inbound + outbound callers/callees per seed and unioned every hop into the envelope; a hotspot seed (a top most-called symbol) at `trace_depth` 2-3 produced a 205 KB response the host cannot receive. `neighbors` was already capped at 50, but `callers` / `callees` were unbounded — they now get the same fixed cap, with new `callers_total` / `callees_total` keeping the true union sizes and a `truncated` flag. Third composite fixed for this class after `plan_change` (#1727) and `investigate_failure` (#1723). Found dogfooding the composite-tool surface.
