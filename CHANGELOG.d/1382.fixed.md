Doctor (CLI + MCP) now tags extraction_failures rows with `is_stale: true` when `last_seen_at` predates the project's `indexed_at` — distinguishes "failing now" from "awaiting next index pass to clear". Pre-fix, fixed-but-not-yet-re-indexed projects surfaced rows that PruneExtractionFailuresForFile (#1319) couldn't clear (the prune only fires on the project's own re-extraction). Dogfood repro: workfixd TSX qualified_name_collision rows lingered for hours after #1377 shipped because workfixd hadn't been re-indexed since the fix, while pincher-repo's docs/index.html row cleared correctly on its own re-index. The new `is_stale` boolean is omitempty so JSON consumers see no change for fresh failures; stale rows now carry the explicit tag. Helper mirrored in both `internal/server/admin.go` and `cmd/pinch/project.go` per the bounded-duplication convention. #1382.
