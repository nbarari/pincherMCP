`neighborhood` now scopes to the session project FIRST when no `project=` arg is passed — same fix the symbol + context handlers got in #1409 but missed for neighborhood because its lookup path lives in a separate file from `handleSymbol`'s switch. Pre-fix repro on the dogfood DB (which had pincher-repo, sniffer, and pincherMCP all indexed with the same source tree): `mcp__pincher__neighborhood id="internal/server/server.go::server.*Server.handleSearch#Method"` would error with `seed is not in the session project pincher-repo — it exists only in project sniffer` even though the seed IS in pincher-repo. The unscoped `GetSymbol` fallback hit sniffer's row first; the #1232 strict-cross-project guard then fired pointing AWAY from the session project. Now follows the #1408/#1409 shape: `GetSymbolScoped(s.sessionID, id)` first, fall back to unscoped only when truly not in session — so the strict guard's "exists only in X" error means what it says. New `neighborhood_scope_to_session_test.go` covers the positive case (seed in both projects → prefer session) and the negative case (seed only in other project → strict guard still fires). (#1425)
