**Tutorial anchor links and tool-count parity test ([#672](https://github.com/kwad77/pincher/issues/672) workstream 4 v0.79 hardening):** three tutorial pages (`docs/tutorials/claude-code.md`, `cursor.md`, `vscode-copilot.md`) linked to `#the-22-mcp-tools` — a stale anchor that no longer resolved in REFERENCE.md (current heading is `## The 23 MCP tools` since v0.65 #1192/#1191). Users clicking those links landed at the top of REFERENCE.md instead of the tool catalogue, looking like a 404 to the anchor-jumping behaviour they expected. New `TestReferenceMD_ToolCountParity` pins REFERENCE.md's `**MCP tools:** N` metadata line + `## The N MCP tools` heading against runtime `len(srv.tools)`; new `TestTutorials_ToolCountAnchorParity` walks `docs/tutorials/*.md` and asserts every `#the-N-mcp-tools` anchor matches REFERENCE.md's heading. Drift had shipped through six release cycles undetected.
