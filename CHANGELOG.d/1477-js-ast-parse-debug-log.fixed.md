`parseJSWithRecovery` now logs at `slog.Debug` when the JS parser rejects source (#1477 v0.84). Pre-fix the silent fallback from AST to regex extraction left users debugging "why are my JS symbols at regex confidence (0.95/0.975)" with no diagnostic surface. Enable via `PINCHER_LOG_LEVEL=debug` to see which files trigger the fallback. Production logging stays quiet by default. Diagnostic surface only — the followup wiring of `extraction_failures` rows + `health._meta.warnings` for parser-fallback visibility remains tracked in #1477 since it requires plumbing the store ref into the AST extractor.
