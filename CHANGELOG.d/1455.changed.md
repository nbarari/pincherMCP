GitHub Pages landing page (`docs/index.html`) restructured around the three-leverage-layer frame shipped in v0.73 (#1392/#1394). The page now ladders pitch → grab story (95% stats) → retention story (Hook + `_meta` + Composite cards) → worked example (same investigation shown three ways: ~45K tokens atomic-only / ~3.2K atomic+`_meta` / ~2.8K hook+composite) → storage internals → tools → install → roadmap. Hero sub-line names the foundation/enabler/router framing inline. The "Storage" section's badges renamed from `Layer 1/2/3` → `store / graph / search` so they no longer collide vocabulary-wise with the new leverage layers. Install section adds a `pincher init --target=<host>` matrix covering claude / cursor / zed / codex / vscode-copilot / jetbrains / antigravity / windsurf / gemini (was: Claude Code + Cursor/Zed only). Roadmap trimmed from 6 cards to 3 (Phase 3 today / v1.0 freeze / what we're NOT building) — dropped inside-baseball cards that re-state stats or duplicate the new leverage-layer section. **Comparison table deleted** — head-to-head matrices against unnamed alternatives violate the no-comparison-docs-pre-1.0 framing discipline. Footer adds `REFERENCE.md`, `integrations/loop-leverage-layers.md`, and `migration/v0.4-to-v1.0.md` links. Unused `.comparison` CSS block removed. (#1455)
