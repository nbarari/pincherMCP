CLAUDE.md's "Current schema: **vN**" claim bumped from v30 to v32 (was 2 versions stale). The line's own warning ("was 7 versions stale before #998/#999 caught it") was direct evidence the drift recurs. New `TestClaudeMD_SchemaVersionParity` walks the file and asserts the claim matches `db.CurrentSchemaVersion()` — parallel to #1416's REFERENCE.md guard. (#1418)
