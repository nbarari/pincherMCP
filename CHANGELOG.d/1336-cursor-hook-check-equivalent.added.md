Cursor init target now emits a Read/Grep hook-check equivalent in `.cursor/rules/pincher.mdc` (#1336 v0.86). The PreToolUse hook (#627) redirects Read/Grep on indexed files to pincher tools in Claude Code; Cursor users had no equivalent guidance, so the same friction recurred there. The fresh-write path now prepends a "When to use pincher tools" preamble before the marker block, with explicit `mcp__pincher__context id=<symbol_id> lite=true` and `mcp__pincher__search query=<pattern>` examples. Subsequent `pincher init` runs respect user edits — if the preamble is deleted, it stays deleted (only the marker block round-trips).
