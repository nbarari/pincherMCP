`doctor` extraction_failures rows now carry `binary_version_at_failure` so readers can distinguish "fixed-since-this-binary" rows from "still recurring on the running binary" without cross-referencing CHANGELOG by hand. Schema v33.
