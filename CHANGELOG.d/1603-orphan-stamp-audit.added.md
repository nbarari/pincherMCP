`empty_reason` orphan-stamp audit pinned via `TestEmptyReason_OrphanStampAudit` (#1603 v0.84). Every `EmptyReason*` constant must appear in either `knownStamped` (production handler emits it) or `knownOrphan` (catalog entry exists but no handler stamps it). Current state: 9 stamped, 4 orphan (stale_index, unsupported_language, low_confidence_extractor, same_file_only). Test surfaces the orphan count so a future "we forgot to stamp X" regression is visible at PR review; the orphan set must only shrink. Adding `EmptyReasonTargetNotResolved` to `validEmptyReasons` retroactively (forgotten in v0.82's #1578).
