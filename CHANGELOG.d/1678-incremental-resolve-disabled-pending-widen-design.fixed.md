Incremental-resolve scoping disabled pending referrer-widen design (#1678 v0.87). The #1629 slices + #1676 scope-fix delivered a measured 3.2× faster incremental watcher tick on pincher-repo, but dogfood test sweep surfaced a correctness regression: `TestIndex_PendingEdges_PreservesCrossFileCALLSWithoutInvalidation` (#457) failed because the scope didn't include "files whose pending_edges reference re-extracted symbols." When callee B re-extracts and caller A is hash-skipped, the cascade delete on B's symbols wipes the resolved A→B edge, and A's pending_edges are out of scope so the resolver doesn't re-bind them. The Bar→Foo edge was permanently lost — a regression of the v0.16 #457 contract that pending_edges PRESERVE cross-file resolution without invalidation. Proper fix needs a pre-extraction snapshot of the edge graph (cascade has already fired by the time `FilesWithEdgesToFile` runs in the resolve block), filed as #1678. Until that lands, `useResolveScope` is gated off and the resolver runs project-wide every tick. The infrastructure (Store primitives `LoadPendingEdgesByKindAndFiles` / `DeleteEdgesByKindAndSourceForSourceFiles`, `scopeFiles` parameter on all four resolvers) stays in place for the follow-up. Net: incremental tick latency reverts to the pre-#1629 baseline pending the widen design.
