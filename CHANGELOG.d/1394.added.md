`docs/integrations/meta-envelope-contract.md` lands — the integrator-facing per-field contract for the `_meta` envelope (`next_steps`, `empty_reason`, `warnings_v2`, `capabilities`, `request_id`, drift fields). Names the planning-loop input surface that v1.0 will freeze. Pulled forward from v0.78 to ship as one coherent docs body with #1332 + #1392.
