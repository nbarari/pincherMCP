`search` now auto-retries with a trailing `*` wildcard when a bare-identifier query returns zero results (#1643 v0.86). Mirrors the existing AND→OR (#453) and corpus (#113) fallthrough patterns: when an exact-identifier query (`isExactIdentifierQuery`) zero-results after the corpus + AND→OR fallthroughs, the handler tries `<query>*` once across the eligible corpora before falling into the empty-result diagnosis. Surfaces `_meta.fellthrough_to_wildcard=true` + `_meta.effective_query=<wildcard>` so the agent knows the recovery happened and can re-issue the wildcard form directly. Closes a recurring dogfood gap where `suggestEmptyTrace` zero-resulted but `suggestEmptyTrace*` hit on the same call.
