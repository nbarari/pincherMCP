**Cancellation test flake fix (#1569).** `TestCancellation_HandleFetch_ReturnsPromptly` now uses a closed local-port URL (`http://127.0.0.1:1/cancelled`) instead of `https://example.com/docs` and a 1500ms budget (up from 500ms). Original setup let DNS + TCP + TLS handshake latency on slow CI runners dominate before the cancelled-ctx check could fire, producing macOS-specific flakes. The cancellation contract assertion is unchanged.
