`pincher init --target=antigravity` writes `./.antigravity/rules.md` — project-rules path for Google Antigravity (Gemini-3 IDE announced late 2025). Detection fires on `.antigravity/` so `pincher init --target=detect` surfaces Antigravity alongside the other detected hosts. `--global` is rejected with a message routing users to Antigravity's preferences UI; matches the cursor / windsurf / jetbrains pattern. Writer is MergePolicyBlockBare — re-runs replace the marker block in place (idempotent), and `--force` overrides the existing block wholesale. Mirrors the JetBrains target (#1335) shape. Closes #1368.
