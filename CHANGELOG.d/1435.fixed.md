`init` rich-error `next_steps[].args` no longer breaks JSON when `project_path` contains backslashes (Windows paths). Pre-fix the args were string-templated, leaving `D:\ClaudeCode\repo` embedded as invalid JSON escapes; now marshalled via `json.Marshal` so every shell escapes correctly across platforms (#1435).
