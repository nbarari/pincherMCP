Split `queries_zero_result` into audit-shape vs caller-surprised — schema v34 (#1632 v0.85). Two new sessions columns (`queries_zero_expected`, `queries_zero_unexpected`) track which population each zero-result call lands in; `pincher stats` surfaces them alongside the existing total and adds a new `zero_unexpected_rate` JSON field (`queries_zero_unexpected / queries_total`). The classifier: `query` tool with pinchQL/cypher containing a `{...}` property predicate is "expected zero" (audit-shape on a healthy codebase); everything else is "unexpected zero" (the friction signal — agent likely falls back to Read/Grep next). On real dogfood data the headline `zero_result_rate` of 77% conflated 70% audit-shape (clean codebase) with 7% unexpected; the new metric exposes the dial v1.0 needs to drive usage frequency. Closes #1494 half 1.
