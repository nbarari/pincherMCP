C and C++ extractor (shared `cRE` extractor, dispatched on `.c` / `.h` / `.cpp` / `.cxx` / `.cc` / `.hpp` / `.hh`) promoted from confidence **0.70 → 0.85** (stable-regex tier). Adds `class Name : public Base` (C++) and `struct Name : Base` declarations with inheritance lists, `struct Name { ... }` for C (kernel / embedded code commonly uses bare struct types — `file_operations`, `list_head`), `union Name { ... }` (modeled as Class), `enum Name { ... }` (anonymous + named), and C++11+ `enum class Name` and `enum struct Name` strongly-typed scoped enums. Name patterns tolerate both lowercase-first (C snake_case convention) and PascalCase (C++ convention). Inheritance lists tolerate access specifiers (`public`, `private`, `protected`, `virtual`) and namespace-qualified parent types (`base::Class`). Existing function/method extraction + Linux-kernel macro-rewrite post-processing (#69/#73/#324 `cMacroRE`) stay unchanged. Closes the v0.73 promotion wave — all five remaining 0.70-tier languages (Swift #1450, Kotlin #1457, C# #1459, PHP #1461, C/C++ #1463) now ship at stable-regex 0.85. **What's still out of scope** (regex-tier ceiling): C++ namespaces (multi-block scope tracking), templates (`template<typename T>` multi-line prefix), operator overloads (`operator+`, `operator[]`), `extern "C"` linkage blocks. AST-tier (1.0) C/C++ tracked separately for a v0.85+ slot via pure-Go cc parser (modernc.org/cc). (#1463)
