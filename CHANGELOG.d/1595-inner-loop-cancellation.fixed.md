`investigate_failure` + `onboard_module` honor `ctx.Err()` in inner loops (#1595 v0.83). #1579 covered the entry-point + per-suspect/per-candidate fan-out loops in v0.82, but EXPLORE-mode dogfood surfaced 4 more long-running inner loops that ran to completion after cancel: `investigate_failure` Step 2 frame-search + Step 6 callers union; `onboard_module` Step 1 scope rows + Step 3 edges rows. Check-every-100-rows pattern on the rows.Next() loops bounds latency without per-row overhead.
