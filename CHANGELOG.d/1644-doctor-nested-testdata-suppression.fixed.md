`pincher doctor`'s nested-project advisory now suppresses intentional nesting under `testdata/corpus/*` (pinned corpus fixtures required by `make corpus-test`), `testdata/__fixtures__/*` (the broader trace-fixture convention), and `.atrium/work/*` (slopbuster worktrees). The advisory's remediation suggestion — `pincher project rm <inner>` — would have broken the corpus snapshot gates if a user followed it. Genuine nested-project mistakes (e.g. `warp-fork` indexed under `warp_rc`) still fire as before (#1644 v0.85).
