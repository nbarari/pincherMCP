`docs/integrations/loop-leverage-layers.md` lands — the conceptual frame naming pincher's three agent-loop leverage layers (hook + `_meta` + composite). Establishes the vocabulary the migration guide and the `_meta` envelope contract page both reference. Pulled forward from v0.75 to ship as one coherent docs body with #1332 + #1394.
