Host-conformance corpus extended from one host to all six (#1562–#1566 v0.88). v0.87's FILE-M (#1532) shipped the corpus with `claude-code` as the working reference; the runner (`scripts/host-conformance.sh`) replays a recorded `initialize → tools/list → search → architecture` JSON-RPC transcript against a freshly-built pincher and asserts the response shape. This adds the five remaining supported hosts — `cursor`, `codex`, `jetbrains`, `vscode-copilot`, `zed` — each with a `workflow.jsonl` + `expectations.json` under `testdata/host-conformance/<host>/`. The five v0.88 hosts ship as canonical wire-shape transcripts (the claude-code reference sequence with the host's own `clientInfo.name`) — they exercise pincher's protocol surface so a future tool-shape change can't silently break a host's canonical workflow. Replacing any with a live host capture is a strict improvement and stays compatible with the runner + the v0.91 release-gate (#1536, FILE-R) since the assertion schema is identical. With all six hosts present, `scripts/host-conformance.sh all` now covers the full supported matrix.
