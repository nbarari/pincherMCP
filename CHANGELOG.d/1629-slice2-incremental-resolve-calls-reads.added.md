Incremental resolve pass extended to CALLS + READS (#1629 v0.87, slice 2). Slice 1 (PR #1673) shipped the IMPORTS resolver + the foundational store primitives. This slice extends the same scoping pattern to `resolveCalls` (the biggest stage at 252ms on pincher-repo's measured watcher tick) and `resolveReads` (181ms) — together with slice 1's IMPORTS work, the full resolve block now skips work on unchanged files when totalFiles ≤ 32 AND !force. New generalized `DeleteEdgesByKindAndSourceForSourceFiles` Store method handles both `resolve_pass` (CALLS / READS / WRITES) and `binding_pass` (function-value CALLS produced by resolveReads) source kinds. `resolveImports` / `resolveCalls` / `resolveReads` all now accept a `scopeFiles []string` parameter — nil/empty preserves the project-wide delete + rebuild behavior; non-empty wipes only the scoped files' resolved edges and trusts the caller to pre-filter `pending` to the same scope. The previous IMPORTS-only conditional in `Index()` collapses into a single `useResolveScope` decision applied to all three loaders.
