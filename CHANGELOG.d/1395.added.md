**docs:** Use-case pricing framework at `docs/integrations/use-case-pricing.md` ([#1395](https://github.com/kwad77/pincher/issues/1395)). Defines the four serious-use-case scenarios (find-dead-code / why-test-failing / blast-radius / orient-on-module) plus one negative-control scenario (format-this-file) that the Phase 4 composite-tool work gets priced against. Per-scenario three-dimensional measurement methodology: token cost (input+output), wall-clock latency (p50+p95 — p95 is the trap detector), accuracy (manually labeled reference sets per scenario, small N high-quality). Atomic baselines named today; composite cells fill in as composites ship v0.81+. Establishes the per-doc citation policy: every claim in composite-tool-roadmap / loop-leverage-layers / meta-envelope-contract about composite-vs-atomic deltas must cite a specific Scenario N row, no abstract "5-10 calls collapse" claims permitted. Closes the precedent gap Sourcegraph et al. had with "published 10×, measured 2×" — credibility before marketing.
