Standard-tier atomic tools now honor `ctx.Err()` at handler entry (#1601 v0.84). Closes the FILE-H follow-up for `search`, `trace`, `query`, `changes`, `architecture`, `neighborhood`, and `context` — each gets an entry-point cancel check matching the v0.82 #1579 composite pattern. New `TestCancellation_StandardTierDBToolsHaveContractTest` gate enforces the contract going forward; 5 new `TestCancellation_HandleX_ReturnsPromptly` tests pin each handler at <500ms on a pre-cancelled context.
