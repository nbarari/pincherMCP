**Host conformance corpus + replay runner (FILE-M, #1532).** New `testdata/host-conformance/<host>/` directory layout with `workflow.jsonl` (recorded MCP JSON-RPC transcript) and `expectations.json` (assertions over response shape + required tool list + regex over semantic content). `scripts/host-conformance.sh` replays the per-host transcript against a freshly-built pincher in stdio mode and validates responses; advisory CI gate at `.github/workflows/host-conformance.yml` runs on every PR + master push + dispatch. Promotes from advisory to release-blocker at v0.91 per FILE-R (#1536) by editing one `continue-on-error` line. Initial scope ships the `claude-code` host as a working reference; remaining hosts (cursor, codex, jetbrains, vscode-copilot, zed) ship between v0.87 and v0.91. Methodology documented in [docs/methodology/host-conformance.md](docs/methodology/host-conformance.md).
