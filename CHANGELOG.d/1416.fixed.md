`docs/REFERENCE.md` Schema section caught up to runtime — the inline "Current version: **vN**" claim was 6 versions stale (v26 vs actual v32) and the migration table was missing 6 rows (v26→v27 through v31→v32). New `TestReferenceMD_SchemaVersionParity` walks the file and asserts both the inline claim and every migration row match `db.CurrentSchemaVersion()` and the `schemaMigrations` slice — same mechanical drift-guard as the tool-contract gate. (#1416)
