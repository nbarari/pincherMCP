`handleIndex`: entry-point `ctx.Err()` check, mirroring the rest of the atomic standard-tier surface. The #1601 v0.84 cancellation sweep covered `handleSearch` / `handleTrace` / `handleChanges` / `handleArchitecture` / `handleQuery` / `handleContext` but missed `handleIndex` — which relied on the per-iteration check inside `idx.Index`. On a pre-cancelled context, the pre-Index work (arg parse, IsBloatTrap, drift refusal, mustProject, db.Open) ran to completion before the deep ctx check bit; blew the 500ms contract bound on slow runners. `TestCancellation_HandleIndex_ReturnsPromptly` now returns in <30ms (down from >1000ms on Windows). Closes the cancellation-contract uniformity gap.
