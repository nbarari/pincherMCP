Per-host benefits pages in `docs/integrations/` (#1393 v0.88). Each supported MCP host — Claude Code, Cursor, Zed, JetBrains AI Assistant, Codex, VSCode Copilot — gets a `docs/integrations/<host>/benefits.md` page: the *why* (what pincher concretely changes for that host's agent) paired with the existing `docs/tutorials/<host>.md` *how*. The pages are grounded in pincher's own measured numbers (cold index ~900ms, incremental re-index <50ms, `search` ~1ms, `trace` depth-3 <5ms on the 221-file self-corpus) and the host-conformance-verified canonical workflow — no hand-wavy "faster" claims. Claude Code / Cursor / Zed / JetBrains get the full treatment (host-specific integration mechanism + worked benefit framing); Codex and VSCode Copilot get the scaled-down bullet form per the issue. A new `docs/integrations/README.md` hub indexes the benefit pages alongside the existing design-rationale docs. External comparator measurements (#1298 `pincher bench §2`) are a noted follow-up, not a blocker — the pages cite pincher's own bench numbers, which the issue's "real measurements" requirement is satisfied by.
