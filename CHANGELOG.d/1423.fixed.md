Go extractor's READS-pass no longer emits bare-name reads for parameters, receivers, or in-body locals — the #565 binding-pass had been converting those into false CALLS edges whenever the local name happened to match a project Function with the same name. Headline repro: `finishIndexSpan(totalSymbols int)` had a phantom CALLS edge to the `totalSymbols` test helper in `hcl_integration_test.go` simply because the parameter name shadowed the helper. Polluted `trace` (test helper looked called from production) and `dead_code` (test helper looked "live" via the spurious caller). Selector reads (`local.Field`) still emit so the #760 struct-field-type path keeps working; real call sites still emit CALLS regardless of param shadowing. New `go_param_shadow_test.go` covers param shadow, receiver shadow, in-body local shadow, real-call-still-emits-CALLS, package-var-read-still-emits regression, and selector-on-local cross-check. (#1423)
