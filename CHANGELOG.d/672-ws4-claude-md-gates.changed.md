**CLAUDE.md required-gates list parity ([#672](https://github.com/kwad77/pincher/issues/672) workstream 4 v0.79 hardening):** the `PR title/body issue refs` job in `.github/workflows/ci.yml` (runs `scripts/pr-issue-consistency.sh` on every pull_request) is a required gate — no `continue-on-error`, blocks merge — but `CLAUDE.md` § CI conventions' required-gates list named only 7 of 8 gates. Added the missing entry with a one-line behavior note. Doc-only fix; the gate itself was already enforced.
