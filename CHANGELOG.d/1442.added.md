`neighborhood` accepts a `fields` projection (`fields=id,name,kind,start_line`) so the in-file-outline workflow drops the heavy signature + qualified_name + extraction_confidence shape — 3-4× payload reduction on metadata-only calls. Mirrors `search`'s projection. `include_source=true` still gates source disk read; passing `fields=id,source` without `include_source=true` won't trigger the read by accident (#1442 Part 1; Part 2 — token-aware truncation — is a separate change).
