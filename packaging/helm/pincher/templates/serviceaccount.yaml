{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pincher.serviceAccountName" . }}
  labels:
    {{- include "pincher.labels" . | nindent 4 }}
{{- end }}
